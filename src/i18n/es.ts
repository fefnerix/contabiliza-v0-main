/**
 * Textos en español neutro (LATAM) — fuente usada por `@/translations/es`.
 * El hook `useTranslations()` expone `t()` desde PreferencesContext, que resuelve claves contra estas cadenas.
 */
export { default } from '../translations/es';
